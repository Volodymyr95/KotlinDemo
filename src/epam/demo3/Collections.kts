

var immutList = listOf("one","two","three")
//immutList.add("four")

var mutList  = mutableListOf("one","two","three")
mutList.add("four")

var unmutSet = setOf("one", "two")


var mutSet = mutableSetOf("one","two")
mutSet.add("four")

var map = mapOf("one" to "sdhbsdh")

println(map["one"])
